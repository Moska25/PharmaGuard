# PharmaGuard — TODO

Repo: `/Users/sandroama/Documents/GitHub/PharmaGuard`
Stack: Python 3.12 · PyQt5 · SQLite · APScheduler · matplotlib
Status: **runs, 112 tests passing, security issues fixed.** Remaining work is
visual identity, packaging, and one decision only Sandro can make.

Environment is already built: `.venv/` exists with all dependencies.

```bash
.venv/bin/python seed_demo_data.py --reset     # 5 synthetic patients, 288 records
.venv/bin/python main.py                       # Admin1 / PharmaGuard!Demo1
QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest tests/ -q
```

**Do not run any git command.** Leave the tree dirty; Sandro commits.

---

## Done

- [x] **PG-1** Leaked credential DB untracked; PBKDF2-HMAC-SHA256 (240k iterations,
      per-user salt) replacing unsalted SHA-256; legacy hashes still verify and are
      transparently upgraded on next successful login; admin credentials moved to
      `PHARMAGUARD_ADMIN_USER` / `PHARMAGUARD_ADMIN_PASSWORD` with a labelled demo
      fallback; `hmac.compare_digest` throughout.
- [x] **PG-2** `assets/` restored — `logo.svg` authored, `logo.png` and a
      multi-size `pharmaguard.ico` generated by `tools/build_icons.py`.
      `Requirments short.txt` and `GITHUB_UPLOAD_GUIDE.md` deleted.
- [x] **PG-3** `seed_demo_data.py` — deterministic synthetic data so the repo
      never needs to ship a database.
- [x] **PG-4** Admin password reset no longer assigns a fixed `Patient123!` to
      every account; `generate_temporary_password()` produces a unique
      policy-compliant one-time password.
- [x] **PG-5** Scheduler rewritten from exact-minute equality to time windows —
      a delayed tick no longer silently drops a reminder forever. Injectable
      reference time for testing.
- [x] **PG-6** Fixed a crash: a bare-hour time like `"8"` passed through
      `normalized_medicine_time()` unpadded and blew up `strptime`.
- [x] **PG-7** 112 tests across auth, database, medication maths, scheduler.
- [x] **PG-8** Notification sounds generated by `tools/build_sounds.py` (stdlib
      `wave`, no dependency) — the four WAVs the app referenced were absent.
- [x] **PG-9** Screenshots via `tools/capture_screenshots.py`, both themes.
- [x] **PG-10** README rewritten: architecture, design decisions, limitations.

---

## PG-11 (P1) · Visual identity: "Clinical Instrument"

**Read `MOSKA_MAIN/shared/UI_IDENTITIES.md` first** — the palette and the
distinctive moves are specified there. Extend `styles.py`; do not rewrite it.

- [ ] Apply the token palette to both `LIGHT_STYLE` and `DARK_STYLE`
- [ ] Status as a 4px coloured left edge on medication rows, replacing the
      plain text status column as the primary signal (keep the text for
      accessibility — never colour alone)
- [ ] Monospace, tabular figures for every time, dosage, and count
- [ ] Flat cards: 1px border, no drop shadow, 8px grid spacing
- [ ] Thin teal top rule on the active tab instead of a filled tab
- [ ] Visible focus rings on every input and button
- [ ] Rewrite empty states — "No records found." should say what to do next
- [ ] Regenerate screenshots afterwards, both themes

**Acceptance:** side-by-side before/after screenshots; the app must not look
like default Qt. All 112 tests still pass.

## PG-12 (P2) · Statistics tab polish

- [ ] Charts inherit the theme (currently hardcoded `#1E293B` / `#ffffff` in
      `statistics_window.py:_create_chart_container`)
- [ ] Default the tab to "All Patients / This Month" so it is never empty on open
- [ ] Add an adherence-over-time line chart; the current pie says little

## PG-13 (P2) · macOS behaviour

The app is Windows-first and was verified to *launch* on macOS, but:

- [ ] `plyer` notifications untested on macOS — verify or guard
- [ ] `QSoundEffect` playback untested on macOS
- [ ] `"Segoe UI"` is hardcoded and missing on macOS (Qt logs a warning every
      run) — add a platform font fallback chain

## PG-14 (P3) · Windows release build

- [ ] PyInstaller spec, one-file build, icon embedded
- [ ] Attach the `.exe` to a GitHub release — this app cannot be hosted, so a
      download is the only way a reviewer runs it without a Python toolchain

## PG-15 (P3) · Documentation

- [ ] `docs/USER_GUIDE.md` covering both the admin and patient flows. The old
      README claimed `documents/Admin_Manual.md` and `documents/User_Manual.md`,
      neither of which ever existed.

---

## 🚨 PG-16 (P0) · Needs Sandro's decision — the leak is still in GitHub history

Untracking `pharma_guard.db` does **not** remove it from the pushed history.
The file remains fetchable from earlier commits at `github.com/Moska25/PharmaGuard`,
containing 10 real names and unsalted SHA-256 password hashes. Two hashes were
identical, indicating a shared or reused password.

A backup of the original is at `~/Downloads/pharma_guard.LEAKED-BACKUP.db`.

Options:
1. **Delete the GitHub repo and re-push a clean single-commit history** —
   recommended; there are no stars, forks, or issues to lose.
2. `git filter-repo --path pharma_guard.db --invert-paths` then force-push.
3. Accept it, having rotated the passwords.

**Regardless of choice: every password in that file must be treated as compromised.**
